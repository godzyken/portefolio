# Dart/Flutter MCP Server

This directory contains configuration for the Dart/Flutter Model Context Protocol (MCP) server.
It allows AI agents to interact with the project symbols, run tests, and diagnose issues.
